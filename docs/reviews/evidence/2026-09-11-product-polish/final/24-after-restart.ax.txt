Window: "Cairn", App: Cairn.
0 standard window Cairn, Secondary Actions: Raise
	1 split group
		2 split group
			3 text Files
			4 button Description: Collapse Files, Value: Expanded
			5 button Collapse all files
			6 scroll area Secondary Actions: Scroll Up, Scroll Down
				7 outline Files
					8 row (selectable, collapsed) Description: Folder, Value: .claude, Secondary Actions: Expand
						9 disclosure triangle 0
					10 row (selectable, collapsed) Description: Folder, Value: .codex-trace, Secondary Actions: Expand
						11 disclosure triangle 0
					12 row (selectable, collapsed) Description: Folder, Value: .zcache, Secondary Actions: Expand
						13 disclosure triangle 0
					14 row (selectable, expanded) Description: Folder, Value: crates, Secondary Actions: Collapse
						15 disclosure triangle 1
					16 row (selectable, collapsed) Description: Folder, Value: ai, Secondary Actions: Expand
						17 disclosure triangle 0
					18 row (selectable, expanded) Description: Folder, Value: knuth-agent, Secondary Actions: Collapse
						19 disclosure triangle 1
					20 row (selectable, collapsed) Description: Folder, Value: examples, Secondary Actions: Expand
						21 disclosure triangle 0
					22 row (selectable, expanded) Description: Folder, Value: src, Secondary Actions: Collapse
						23 disclosure triangle 1
					24 row (selectable, collapsed) Description: Folder, Value: hooks, Secondary Actions: Expand
						25 disclosure triangle 0
					26 row (selectable, collapsed) Description: Folder, Value: tools, Secondary Actions: Expand
						27 disclosure triangle 0
					28 row (selectable) Description: File, Value: actor.rs
					29 row (selected) Description: File, Value: agent_step.rs
					30 row (selectable) Description: File, Value: event_log.rs
					31 row (selectable) Description: File, Value: harness.rs
					32 row (selectable) Description: File, Value: lib.rs
					33 row (selectable) Description: File, Value: Cargo.toml
					34 row (selectable, collapsed) Description: Folder, Value: knuth-cli, Secondary Actions: Expand
						35 disclosure triangle 0
					36 row (selectable, collapsed) Description: Folder, Value: knuth-core, Secondary Actions: Expand
						37 disclosure triangle 0
					38 row (selectable, collapsed) Description: Folder, Value: docs, Secondary Actions: Expand
						39 disclosure triangle 0
					40 row (selectable) Description: File, Value: .env
					41 row (selectable) Description: File, Value: .gitignore
					42 row (selectable) Description: File, Value: Cargo.lock
					43 row (selectable) Description: File, Value: Cargo.toml
				44 scroll bar (settable, float) 0
					45 value indicator (settable, float) 0
					46 increment arrow button
					47 decrement arrow button
					48 increment page button
					49 decrement page button
			50 splitter (disabled, settable, float) 410
			51 text Outline
			52 button Description: Collapse Outline, Value: Expanded
			53 button Collapse all outline
			54 scroll area Secondary Actions: Scroll Up, Scroll Down
				55 outline Outline
					56 row (selectable, expanded) Secondary Actions: Collapse, Description: Enum AgentStepError
Enum AgentStepError
						57 disclosure triangle 1
					58 row (selectable) Value: (String), Description: Enum case EventSendError
Enum case EventSendError
					59 row (selectable) Value: (String), Description: Enum case LLMError
Enum case LLMError
					60 row (selectable, expanded) Secondary Actions: Collapse, Description: Enum AgentStepActorMessage
Enum AgentStepActorMessage
						61 disclosure triangle 1
					62 row (selectable) Value: (ai::AssistantMessageEvent), Description: Enum case StreamEvent
Enum case StreamEvent
					63 row (selectable) Enum case StreamEnded
Enum case StreamEnded
					64 row (selectable) Enum case Cancel
Enum case Cancel
					65 row (selectable, expanded) Secondary Actions: Collapse, Description: Struct AgentStepActor
Struct AgentStepActor
						66 disclosure triangle 1
					67 row (selectable) Value: : Model, Description: Field model
Field model
					68 row (selectable) Value: : StreamOptions, Description: Field options
Field options
					69 row (selectable) Value: : mpsc::Sender<AgentActorMessage>, Description: Field store_tx
Field store_tx
					70 row (selectable) Value: : ai::Context, Description: Field context
Field context
					71 row (selectable) Value: : StepId, Description: Field step_id
Field step_id
					72 row (selectable) Value: : Generation, Description: Field generation
Field generation
					73 row (selectable) Value: : ModelStepEndReason, Description: Field step_reason
Field step_reason
					74 row (selectable) Value: : Option<AssistantMessage>, Description: Field assistant_message
Field assistant_message
					75 row (selectable, expanded) Secondary Actions: Collapse, Description: Implementation AgentStepActor
Implementation AgentStepActor
						76 disclosure triangle 1
					77 row (selectable) Value: = AgentStepActorMessage, Description: Type alias Message
Type alias Message
					78 row (selectable) Value: (&mut self, ctx: &mut ActorContext<Self::Message>), Description: Method on_start
Method on_start
					79 row (selectable) Value: ( &mut self, message: Self::Message, _: &mut ActorContext<Self::Message>, ) -> ControlFlow<()>, Description: Method handle
Method handle
					80 row (selectable) Value: (&mut self, ctx: &mut ActorContext<Self::Message>), Description: Method on_stop
Method on_stop
					81 row (selectable, expanded) Secondary Actions: Collapse, Description: Implementation AgentStepActor
Implementation AgentStepActor
						82 disclosure triangle 1
					83 row (selectable) Description: Method new
Method new, Value: ( model: Model, options: StreamOptions, store_tx: mpsc::Sender<AgentActorMessage>, context: ai::Context, step_id: Ste…
					84 row (selectable) Value: (&mut self, event: AgentEvent) -> Result<(), AgentStepError>, Description: Method emit
Method emit
					85 row (selectable) Value: (&mut self, event: LiveEvent) -> Result<(), AgentStepError>, Description: Method emit_live
Method emit_live
					86 row (selectable) Value: ( &mut self, event: ai::AssistantMessageEvent, ) -> Result<(), AgentStepError>, Description: Method handle_event
Method handle_event
					87 row (selectable, expanded) Secondary Actions: Collapse, Description: Struct AgentStepRunner
Struct AgentStepRunner
						88 disclosure triangle 1
					89 row (selectable) Value: : StepId, Description: Field step_id
Field step_id
					90 row (selectable) Value: : Generation, Description: Field generation
Field generation
					91 row (selectable) Value: : ActorRuntime<AgentStepActorMessage>, Description: Field step_runtime
Field step_runtime
					92 row (selectable, expanded) Secondary Actions: Collapse, Description: Implementation AgentStepRunner
Implementation AgentStepRunner
						93 disclosure triangle 1
					94 row (selectable) Description: Method new
Method new, Value: ( step_id: StepId, generation: Generation, model: Model, options: StreamOptions, store_tx: mpsc::Sender<AgentActorMes…
					95 row (selectable) Value: (&self) -> StepId, Description: Method step_id
Method step_id
					96 row (selectable) Value: (&self) -> Generation, Description: Method generation
Method generation
					97 row (selectable) Value: (&self), Description: Method cancel
Method cancel
					98 row (selectable, expanded) Secondary Actions: Collapse, Description: Module tests
Module tests
						99 disclosure triangle 1
					100 row (selectable) Value: () -> Model, Description: Function mk_model
Function mk_model
					101 row (selectable) Value: (stop_reason: StopReason, content: Vec<ContentBlock>) -> AssistantMessage, Description: Function mk_message
Function mk_message
					102 row (selectable) Value: (store_tx: mpsc::Sender<AgentActorMessage>) -> AgentStepActor, Description: Function mk_actor
Function mk_actor
					103 row (selectable) Value: (), Description: Function length_done_reason_ends_turn_without_panic
Function length_done_reason_ends_turn_without_panic
					104 row (selectable) Value: (), Description: Function tooluse_done_reason_ends_turn_without_panic
Function tooluse_done_reason_ends_turn_without_panic
					105 row (selectable) Value: (), Description: Function start_step_emits_started_and_ended_with_length_reason
Function start_step_emits_started_and_ended_with_length_reason
					106 row (selectable) Value: ( rx: &mut mpsc::Receiver<AgentActorMessage>, ) -> (ModelStepEndReason, Option<AssistantMessage>), Description: Function recv_step_finished
Function recv_step_finished
					107 row (selectable) Value: (), Description: Function stream_ended_without_done_reports_error
Function stream_ended_without_done_reports_error
					108 row (selectable) Value: (), Description: Function stream_ended_after_done_keeps_done_reason
Function stream_ended_after_done_keeps_done_reason
					109 row (selectable) Value: (), Description: Function cancel_message_breaks_with_cancelled_reason
Function cancel_message_breaks_with_cancelled_reason
					110 row (selectable) Value: (), Description: Function shutdown_without_done_reports_cancelled
Function shutdown_without_done_reports_cancelled
					111 row (selectable) Value: (), Description: Function shutdown_after_done_keeps_completed_reason
Function shutdown_after_done_keeps_completed_reason
				112 scroll bar (settable, float) 0
					113 value indicator (settable, float) 0
					114 increment arrow button
					115 decrement arrow button
					116 increment page button
					117 decrement page button
		118 splitter (disabled, settable, float) 188.5
		119 split group
			120 scroll area
				121 radio button harness.rs, Help: /Users/siancao/work/ai/knuth-rs/crates/knuth-agent/src/harness.rs, Value: 0
				122 button Close harness.rs
				123 radio button actor.rs, Help: /Users/siancao/work/ai/knuth-rs/crates/knuth-agent/src/actor.rs, Value: 0
				124 button Close actor.rs
				125 radio button event_log.rs, Help: /Users/siancao/work/ai/knuth-rs/crates/knuth-agent/src/event_log.rs, Value: 0
				126 button Close event_log.rs
				127 radio button spawn_actor, Value: 0
				128 button Close spawn_actor
				129 radio button agent_step.rs, Help: /Users/siancao/work/ai/knuth-rs/crates/knuth-agent/src/agent_step.rs, Value: 1
				130 button Close agent_step.rs
			131 Description: Reading height, Help: Reading height (⌥⌘0/1/2)
				132 radio button Description: Full, Value: 1, Help: Full shows the whole file; Structure folds function bodies; Overview keeps only signatures and top-level items
				133 radio button Description: Structure, Help: Structure folds function bodies and keeps signatures visible, Value: 0
				134 radio button Description: Overview, Help: Overview keeps only signatures and top-level items; folded bodies stay hidden, Value: 0
			135 list Description: File path, Help: /Users/siancao/work/ai/knuth-rs/crates/knuth-agent/src/agent_step.rs
				136 text URL: file:///Users/siancao/work/ai/knuth-rs, Value: knuth-rs
				137 text URL: file:///Users/siancao/work/ai/knuth-rs/crates, Value: crates
				138 text URL: file:///Users/siancao/work/ai/knuth-rs/crates/knuth-agent, Value: knuth-agent
				139 text URL: file:///Users/siancao/work/ai/knuth-rs/crates/knuth-agent/src, Value: src
				140 text URL: file:///Users/siancao/work/ai/knuth-rs/crates/knuth-agent/src/agent_step.rs, Value: agent_step.rs
			141 scroll area Secondary Actions: Scroll Left, Scroll Right, Scroll Up, Scroll Down
				142 text entry area use std::ops::ControlFlow;

use ai::{AssistantMessage, DoneReason, Model, StreamOptions, stream};
use async_trait::async_trait;
use futures::StreamExt;
use knuth_core::{
    AgentEvent, LiveEvent, ModelStepEndReason,
    ids::{Generation, StepId},
};
use tokio::sync::mpsc;
use tracing::debug;

use crate::*;

#[derive(Debug, thiserror::Error)]
pub enum AgentStepError {
    #[error("Failed to send event to store: {0}")]
    EventSendError(String),

    #[error("LLM error: {0}")]
    LLMError(String),
}

#[derive(Debug)]
pub enum AgentStepActorMessage {
    StreamEvent(ai::AssistantMessageEvent),
    StreamEnded,
    Cancel,
}

struct AgentStepActor {
    model: Model,
    options: StreamOptions,
    store_tx: mpsc::Sender<AgentActorMessage>,
    context: ai::Context,
    step_id: StepId,
    generation: Generation,

    step_reason: ModelStepEndReason,
    assistant_message: Option<AssistantMessage>,
}

#[async_trait]
impl Actor for AgentStepActor {
    type Message = AgentStepActorMessage;

    async fn on_start(&mut self, ctx: &mut ActorContext<Self::Message>) {
        let _ = self
            .emit(AgentEvent::ModelStepStarted {
                step_id: self.step_id,
            })
            .await;

        let Some(tx) = ctx.upgrade() else { return };

        let shutdown = ctx.shutdown.clone();
        let context = self.context.clone();
        let model = self.model.clone();
        let options = self.options.clone();

        tokio::spawn(async move {
            let mut stream = stream(&model, &context, Some(&options));
            loop {
                tokio::select! {
                    _ = shutdown.cancelled() => break,
                    event = stream.next() => {
                        match event {
                            Some(event) => {
                                if tx.send(AgentStepActorMessage::StreamEvent(event)).await.is_err() {
                                    break;
                                }
                            }
                            None => {
                                let _ = tx.send(AgentStepActorMessage::StreamEnded).await;
                                break;
                            }
                        }
                    }
                }
            }
        });
    }

    async fn handle(
        &mut self,
        message: Self::Message,
        _: &mut ActorContext<Self::Message>,
    ) -> ControlFlow<()> {
        match message {
            AgentStepActorMessage::StreamEvent(event) => match self.handle_event(event).await {
                Ok(()) => ControlFlow::Continue(()),
                Err(e) => {
                    self.step_reason = ModelStepEndReason::Error(e.to_string());
                    ControlFlow::Break(())
                }
            },
            AgentStepActorMessage::StreamEnded => {
                // A stream that closes without a Done (or Error) event is a broken
                // stream, not a successful step.
                if self.assistant_message.is_none() {
                    self.step_reason =
                        ModelStepEndReason::Error("stream ended without a Done event".to_string());
                }
                ControlFlow::Break(())
            }
            AgentStepActorMessage::Cancel => {
                self.step_reason = ModelStepEndReason::Cancelled;
                ControlFlow::Break(())
            }
        }
    }

    async fn on_stop(&mut self, ctx: &mut ActorContext<Self::Message>) {
        if ctx.shutdown.is_cancelled() && self.assistant_message.is_none() {
            self.step_reason = ModelStepEndReason::Cancelled;
        }

        // Completion is a control signal, not a domain event: the harness
        // derives and commits `ModelStepEnded` itself.
        let _ = self
            .store_tx
            .send(AgentActorMessage::StepFinished {
                step_id: self.step_id,
                generation: self.generation,
                reason: self.step_reason.clone(),
                assistant_message: self.assistant_message.take(),
            })
            .await;
    }
}

impl AgentStepActor {
    pub fn new(
        model: Model,
        options: StreamOptions,
        store_tx: mpsc::Sender<AgentActorMessage>,
        context: ai::Context,
        step_id: StepId,
        generation: Generation,
    ) -> Self {
        Self {
            model,
            options,
            store_tx,
            context,
            step_id,
            generation,
            step_reason: ModelStepEndReason::Success,
            assistant_message: None,
        }
    }

    pub async fn emit(&mut self, event: AgentEvent) -> Result<(), AgentStepError> {
        self.store_tx
            .send(AgentActorMessage::Step(self.step_id, event))
            .await
            .map_err(|e| AgentStepError::EventSendError(e.to_string()))
    }

    /// Reports streaming progress. Routed through the actor mailbox like
    /// durable events so subscribers observe both in emission order.
    async fn emit_live(&mut self, event: LiveEvent) -> Result<(), AgentStepError> {
        self.store_tx
            .send(AgentActorMessage::Live(event))
            .await
            .map_err(|e| AgentStepError::EventSendError(e.to_string()))
    }

    async fn handle_event(
        &mut self,
        event: ai::AssistantMessageEvent,
    ) -> Result<(), AgentStepError> {
        match event {
            ai::AssistantMessageEvent::Start { .. } => {}
            ai::AssistantMessageEvent::Done { message, reason } => {
                self.step_reason = match reason {
                    DoneReason::Stop => ModelStepEndReason::Success,
                    DoneReason::Length => ModelStepEndReason::Length,
                    DoneReason::ToolUse => ModelStepEndReason::ToolUse,
                };
                self.assistant_message = Some(message);
                debug!("Done reason: {:?}", reason);
            }
            ai::AssistantMessageEvent::Error { error, .. } => {
                return Err(AgentStepError::LLMError(
                    error.error_message.unwrap_or("Unknown error".to_string()),
                ));
            }

            ai::AssistantMessageEvent::TextStart { content_index, .. } => {
                self.emit_live(LiveEvent::AssistantMessageTextStarted {
                    step_id: self.step_id,
                    content_index,
                })
                .await?;
            }
            ai::AssistantMessageEvent::TextDelta {
                content_index,
                delta,
                ..
            } => {
                self.emit_live(LiveEvent::AssistantMessageTextDelta {
                    step_id: self.step_id,
                    content_index,
                    delta,
                })
                .await?;
            }
            ai::AssistantMessageEvent::TextEnd {
                content_index,
                content,
                partial,
                ..
            } => {
                self.emit_live(LiveEvent::AssistantMessageTextCompleted {
                    step_id: self.step_id,
                    content_index,
                    text_content: content,
                    assistant_message: partial,
                })
                .await?;
            }

            ai::AssistantMessageEvent::ThinkingStart { content_index, .. } => {
                self.emit_live(LiveEvent::AssistantMessageThinkingStarted {
                    step_id: self.step_id,
                    content_index,
                })
                .await?;
            }
            ai::AssistantMessageEvent::ThinkingDelta {
                content_index,
                delta,
                ..
            } => {
                self.emit_live(LiveEvent::AssistantMessageThinkingDelta {
                    step_id: self.step_id,
                    content_index,
                    delta,
                })
                .await?;
            }
            ai::AssistantMessageEvent::ThinkingEnd {
                content_index,
                content,
                ..
            } => {
                self.emit_live(LiveEvent::AssistantMessageThinkingCompleted {
                    step_id: self.step_id,
                    content_index,
                    content,
                })
                .await?;
            }

            // ignore these events; they are handled by the harness after completion
            // TODO: streaming tool calls need to be handled here?
            ai::AssistantMessageEvent::ToolCallEnd { .. } => {}
            ai::AssistantMessageEvent::ToolCallStart { .. } => {}
            ai::AssistantMessageEvent::ToolCallDelta { .. } => {}
        }

        Ok(())
    }
}

#[derive(Debug)]
pub struct AgentStepRunner {
    step_id: StepId,
    generation: Generation,
    step_runtime: ActorRuntime<AgentStepActorMessage>,
}

impl AgentStepRunner {
    pub async fn new(
        step_id: StepId,
        generation: Generation,
        model: Model,
        options: StreamOptions,
        store_tx: mpsc::Sender<AgentActorMessage>,
        context: ai::Context,
    ) -> Self {
        let step_runtime = spawn_actor(
            AgentStepActor::new(model, options, store_tx, context, step_id, generation),
            100,
        )
        .await;

        Self {
            step_id,
            generation,
            step_runtime,
        }
    }

    pub fn step_id(&self) -> StepId {
        self.step_id
    }

    pub fn generation(&self) -> Generation {
        self.generation
    }

    pub async fn cancel(&self) {
        let _ = self
            .step_runtime
            .handle()
            .send(AgentStepActorMessage::Cancel)
            .await;
    }
}

#[cfg(test)]
mod tests {
    use super::*;
    use ai::providers::faux::{clear_faux_responses, faux_thinking, set_faux_responses};
    use ai::{
        Api, AssistantMessage, AssistantMessageEvent, AssistantRole, ContentBlock, KnownApi,
        ModelCost, Provider, StopReason, ToolCall, Usage,
    };
    use serde_json::Map;
    use std::time::Duration;
    use tokio::time::timeout;

    fn mk_model() -> Model {
        Model {
            id: "faux".into(),
            name: "Faux".into(),
            api: Api::from("faux"),
            provider: Provider::from("faux"),
            base_url: String::new(),
            reasoning: false,
            thinking_level_map: None,
            input: vec![],
            cost: ModelCost::default(),
            context_window: 128_000,
            max_tokens: 4096,
            headers: None,
            compat: None,
        }
    }

    fn mk_message(stop_reason: StopReason, content: Vec<ContentBlock>) -> AssistantMessage {
        AssistantMessage {
            role: AssistantRole::Assistant,
            content,
            api: Api::known(KnownApi::OpenAIResponses),
            provider: Provider::from("openai"),
            model: "test-model".into(),
            response_model: None,
            response_id: None,
            diagnostics: None,
            usage: Usage::default(),
            stop_reason,
            error_message: None,
            timestamp: 0,
        }
    }

    fn mk_actor(store_tx: mpsc::Sender<AgentActorMessage>) -> AgentStepActor {
        AgentStepActor::new(
            mk_model(),
            StreamOptions::default(),
            store_tx,
            ai::Context {
                system_prompt: None,
                messages: vec![],
                tools: None,
            },
            StepId::new(),
            Generation::new(),
        )
    }

    #[tokio::test]
    async fn length_done_reason_ends_turn_without_panic() {
        let (tx, _rx) = mpsc::channel(4);
        let mut step = mk_actor(tx);
        let message = mk_message(StopReason::Length, vec![ContentBlock::text("partial")]);

        step.handle_event(AssistantMessageEvent::Done {
            reason: DoneReason::Length,
            message,
        })
        .await
        .unwrap();

        assert_eq!(step.step_reason, ModelStepEndReason::Length);
        assert_eq!(
            step.assistant_message.unwrap().stop_reason,
            StopReason::Length
        );
    }

    #[tokio::test]
    async fn tooluse_done_reason_ends_turn_without_panic() {
        let (tx, _rx) = mpsc::channel(4);
        let mut step = mk_actor(tx);
        let message = mk_message(
            StopReason::ToolUse,
            vec![ContentBlock::ToolCall(ToolCall {
                id: "call_1".into(),
                name: "search".into(),
                arguments: Map::new(),
                thought_signature: None,
            })],
        );

        step.handle_event(AssistantMessageEvent::Done {
            reason: DoneReason::ToolUse,
            message,
        })
        .await
        .unwrap();

        assert_eq!(step.step_reason, ModelStepEndReason::ToolUse);
        assert_eq!(
            step.assistant_message.unwrap().stop_reason,
            StopReason::ToolUse
        );
    }

    #[tokio::test]
    async fn start_step_emits_started_and_ended_with_length_reason() {
        let _guard = crate::test_support::faux_lock();
        clear_faux_responses();
        let (tx, mut rx) = mpsc::channel(16);
        let message = mk_message(
            StopReason::Length,
            vec![ContentBlock::text("partial"), faux_thinking("reasoning")],
        );
        set_faux_responses(vec![message]);

        let runner = AgentStepRunner::new(
            StepId::new(),
            Generation::new(),
            mk_model(),
            StreamOptions::default(),
            tx,
            ai::Context {
                system_prompt: None,
                messages: vec![],
                tools: None,
            },
        )
        .await;

        let mut messages = Vec::new();
        loop {
            let message = timeout(Duration::from_secs(1), rx.recv())
                .await
                .expect("step should emit events")
                .expect("step channel should stay open until ModelStepEnded");
            let ended = matches!(message, AgentActorMessage::StepFinished { .. });
            messages.push(message);
            if ended {
                break;
            }
        }
        clear_faux_responses();

        let expected_step_id = match messages.first().expect("expected actor messages") {
            AgentActorMessage::Step(
                step_id,
                AgentEvent::ModelStepStarted {
                    step_id: event_step_id,
                },
            ) => {
                assert_eq!(step_id, event_step_id);
                *step_id
            }
            _ => panic!("first message should be ModelStepStarted"),
        };
        // step_id stays readable after the actor has exited.
        assert_eq!(runner.step_id(), expected_step_id);

        for message in &messages {
            match message {
                AgentActorMessage::Step(step_id, event) => {
                    assert_eq!(*step_id, expected_step_id);
                    if let AgentEvent::ModelStepStarted { step_id } = event {
                        assert_eq!(*step_id, expected_step_id);
                    }
                }
                AgentActorMessage::Live(event) => {
                    assert_eq!(event.step_id(), expected_step_id);
                }
                AgentActorMessage::StepFinished { step_id, .. } => {
                    assert_eq!(*step_id, expected_step_id);
                }
                AgentActorMessage::Command(_) => panic!("unexpected actor command"),
                AgentActorMessage::ToolFinished { .. } => panic!("unexpected ToolFinished"),
                AgentActorMessage::ToolInputPrepared { .. } => {
                    panic!("unexpected ToolInputPrepared")
                }
                AgentActorMessage::ToolResultFinalized { .. } => {
                    panic!("unexpected ToolResultFinalized")
                }
            }
        }

        let lifecycle = messages
            .iter()
            .filter_map(|message| match message {
                AgentActorMessage::Step(_, AgentEvent::ModelStepStarted { .. }) => Some("started"),
                AgentActorMessage::StepFinished { .. } => Some("finished"),
                _ => None,
            })
            .collect::<Vec<_>>();
        assert_eq!(lifecycle, ["started", "finished"]);

        let live_events = messages
            .iter()
            .filter_map(|message| match message {
                AgentActorMessage::Live(event) => Some((event.name(), event.step_id())),
                _ => None,
            })
            .collect::<Vec<_>>();
        assert_eq!(
            live_events,
            [
                ("AssistantMessageTextStarted", expected_step_id),
                ("AssistantMessageTextDelta", expected_step_id),
                ("AssistantMessageTextCompleted", expected_step_id),
                ("AssistantMessageThinkingStarted", expected_step_id),
                ("AssistantMessageThinkingDelta", expected_step_id),
                ("AssistantMessageThinkingCompleted", expected_step_id),
            ]
        );

        match messages.last().expect("expected actor messages") {
            AgentActorMessage::StepFinished {
                step_id,
                generation: _,
                reason,
                assistant_message,
            } => {
                assert_eq!(*step_id, expected_step_id);
                assert_eq!(*reason, ModelStepEndReason::Length);
                assert_eq!(
                    assistant_message
                        .as_ref()
                        .expect("StepFinished should include assistant_message")
                        .stop_reason,
                    StopReason::Length
                );
            }
            _ => unreachable!(),
        }
    }

    fn recv_step_finished(
        rx: &mut mpsc::Receiver<AgentActorMessage>,
    ) -> (ModelStepEndReason, Option<AssistantMessage>) {
        loop {
            match rx.try_recv().expect("expected a StepFinished message") {
                AgentActorMessage::StepFinished {
                    reason,
                    assistant_message,
                    ..
                } => return (reason, assistant_message),
                _ => continue,
            }
        }
    }

    #[tokio::test]
    async fn stream_ended_without_done_reports_error() {
        let (tx, _rx) = mpsc::channel(4);
        let mut step = mk_actor(tx);
        let mut ctx = test_actor_context();

        let flow = step
            .handle(AgentStepActorMessage::StreamEnded, &mut ctx)
            .await;

        assert!(flow.is_break());
        assert!(matches!(step.step_reason, ModelStepEndReason::Error(_)));
    }

    #[tokio::test]
    async fn stream_ended_after_done_keeps_done_reason() {
        let (tx, _rx) = mpsc::channel(4);
        let mut step = mk_actor(tx);
        let mut ctx = test_actor_context();

        step.handle_event(AssistantMessageEvent::Done {
            reason: DoneReason::Stop,
            message: mk_message(StopReason::Stop, vec![ContentBlock::text("done")]),
        })
        .await
        .unwrap();
        let flow = step
            .handle(AgentStepActorMessage::StreamEnded, &mut ctx)
            .await;

        assert!(flow.is_break());
        assert_eq!(step.step_reason, ModelStepEndReason::Success);
    }

    #[tokio::test]
    async fn cancel_message_breaks_with_cancelled_reason() {
        let (tx, _rx) = mpsc::channel(4);
        let mut step = mk_actor(tx);
        let mut ctx = test_actor_context();

        let flow = step.handle(AgentStepActorMessage::Cancel, &mut ctx).await;

        assert!(flow.is_break());
        assert_eq!(step.step_reason, ModelStepEndReason::Cancelled);
    }

    #[tokio::test]
    async fn shutdown_without_done_reports_cancelled() {
        let (tx, mut rx) = mpsc::channel(4);
        let mut step = mk_actor(tx);
        let mut ctx = test_actor_context();
        ctx.shutdown.cancel();

        step.on_stop(&mut ctx).await;

        let (reason, assistant_message) = recv_step_finished(&mut rx);
        assert_eq!(reason, ModelStepEndReason::Cancelled);
        assert!(assistant_message.is_none());
    }

    #[tokio::test]
    async fn shutdown_after_done_keeps_completed_reason() {
        let (tx, mut rx) = mpsc::channel(4);
        let mut step = mk_actor(tx);
        step.handle_event(AssistantMessageEvent::Done {
            reason: DoneReason::Stop,
            message: mk_message(StopReason::Stop, vec![ContentBlock::text("done")]),
        })
        .await
        .unwrap();
        let mut ctx = test_actor_context();
        ctx.shutdown.cancel();

        step.on_stop(&mut ctx).await;

        let (reason, assistant_message) = recv_step_finished(&mut rx);
        assert_eq!(reason, ModelStepEndReason::Success);
        assert_eq!(
            assistant_message
                .expect("completed step keeps its message")
                .stop_reason,
            StopReason::Stop
        );
    }
}

				143 scroll bar (disabled, settable, float) 0
				144 scroll bar (settable, float) 0.000931026456668477
					145 value indicator (settable, float) 0.000931026456668477
					146 increment arrow button
					147 decrement arrow button
					148 increment page button
					149 decrement page button
				150 ruler
			151 splitter (disabled, settable, float) 879.5
		152 splitter (disabled, settable, float) 879.5
		153 radio button Description: Callers, Value: 1
		154 radio button Description: Calls, Value: 0
		155 radio button Description: Implements, Value: 0
		156 radio button Description: References, Value: 0
		157 button (disabled) Freeze Results, Description: Freeze Results as Reading Set, Help: Freeze the currently published source and evidence snapshots as a Reading Set tab
		158 button (disabled) Inspector, Description: Show Resolution Inspector, Help: Show Resolution Inspector (⌘I)
		159 split group Description: Relation list and Resolution Inspector, Help: Drag the divider to resize the relation list and inspector
			160 text Right-click a symbol → Show Callers / Calls / Implements / References
		161 splitter (disabled, settable, float) 763
	162 button Context, Description: Show definition context, Help: Show or hide the definition context
	163 text Description: Exact provider status, Value: Exact: ready · Safe (limited), Help: Provider: rust-analyzer
Tool version: rust-analyzer 0.0.0 (9074e9b4c6 2026-09-06)
Trust: Safe
Limitations: build scripts disabled; proc macros disabled
Features: default
	164 button Description: Analysis status details, Help: Analysis status and available features
	165 toolbar
		166 button (disabled) Description: Back, Secondary Actions: Move next, Remove from toolbar
		167 button (disabled) Description: Forward, Secondary Actions: Move previous, Move next, Remove from toolbar
		168 text Description: Project, Value: knuth-rs
		169 button Description: Version, Secondary Actions: Move previous, Move next, Remove from toolbar
		170 button Description: Symbols, Secondary Actions: Move previous, Move next, Remove from toolbar
		171 button Description: Profile, Secondary Actions: Move previous, Move next, Remove from toolbar
		172 button Description: Settings, Secondary Actions: Move previous, Remove from toolbar
	173 close button
	174 full screen button Help: this button also has an action to zoom the window, Secondary Actions: zoom the window
	175 minimize button
176 menu bar
	177 Cairn
	178 File
	179 Edit
	180 Find
	181 Go
	182 View
	183 Relations

The focused UI element is 0 standard window Cairn, Secondary Actions: Raise