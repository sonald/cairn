# V0 原生重跑 — 2026-09-09 19:01–19:07 +0800

源码 HEAD：1cc9da1。沿用 `.build/reliability-ui-validation/Cairn.app`，bundle id `dev.cairn.Cairn.ReliabilityUIValidation`；本轮未重新构建。macOS 26.6.2 (25G83)。fixture `/tmp/cairn-v0-fixture`。

## 已观测

- 当前 CUA AX 与 Cairn 截图可读，旧 Screen Recording/Accessibility denied 阻塞未再出现。Finder 截图先返回纯白，Cairn 截图随后成功；不能将 Finder 结果当成 Cairn 权限失败。
- 鼠标 Open Project → 原生 picker → fixture → 语言弹窗自动勾选 Rust、Python/TypeScript 未勾选 → 文件树 → lib.rs 可读。截图显示 target 与 second_fn 两个函数，AX 内容匹配。
- ⌘O → Return → 语言弹窗 → Return 可重开；Tab/方向键没有让文件树获得可见焦点，纯键盘首开全流程未通过。
- Go → Open Symbol，输入 #target → 返回 src/lib.rs:1:8 → Return 正确定位 target。空 # 的 No project symbols found 不代表输入搜索后无结果。
- EOF：ps 确认 Cairn PID 54544 的完整路径为隔离 bundle，rust-analyzer PID 55257 的 PPID 为 54544，仅向 55257 发 TERM。另一个 Cursor provider 8444 未触碰。
- 断开后 5 秒 sample：主线程 4133/4134 次停在 mach_msg 事件等待；零 readability handler 帧。ps：CPU 0.0%，RSS 146272 KiB。原始采样见 v0-eof-rerun.sample.txt.gz。
- 断开后通过 Go → Command Palette → other.rs → Return 成功打开 other_helper 源码，Reader 仍可用。状态栏仍显示 Exact: ready；未进一步发出 Exact 请求，本轮不判定该状态文案的更新时机。

## 未完成与限制

- 文件树 other.rs 单击两次、坐标点击、Raise 后点击、双击均未改变选中项；菜单与 Quick Open 可用。重复观察与截图确认。尚不能区分 CUA 事件投递与应用点击行为，不能报告鼠标全流程 PASS，也不能断言应用死锁（采样为事件等待）。
- 步骤 1 仅部分；步骤 2 的断开后空转与 Reader 可用性已获得本轮证据。步骤 3–10 的完整矩阵尚未重跑，维持未验收，不再笼统归因为屏幕录制权限。
- 原 fixture asset.png 仅 20 字节，源码函数没有调用链，缺少 PDF；尚未修改。后续正常图片/PDF预览与 Relations 分支需补全 fixture。
- 本轮未建立正式 App Support 操作前指纹，未验证新隔离数据目录首开，因此不声称持久化隔离/零写完整验收通过。源码默认 session 路径按 bundle id 隔离；未预写 session。
- 未运行或更改 /Applications/Cairn.app；未改源码、fixture、.claude-trace/、docs/reviews/；未重跑无代码变化的 CI。
